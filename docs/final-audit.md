# Finale Audit 0.1.0

Deze audit hoort vlak voor de eerste release opnieuw te worden bijgewerkt.

Laatste releasevoorbereiding:

- `AGENTS.md` is opnieuw toegevoegd.
- `.gitignore` is toegevoegd.
- `CHANGELOG.md` is toegevoegd.
- `docs/release-checklist.md` is toegevoegd.
- `index.html`, `eslint.config.js` en de GitHub Pages-workflow zijn aanwezig.

Bekende aandachtspunten:

- `git` is in deze shell niet beschikbaar, dus `git status` moet nog lokaal worden gecontroleerd.
- `content-source/` ontbreekt in deze werkmap.
- Alle ingebouwde vragen zijn nog `reviewed: false`.
- Een echte browserconsolecheck blijft handmatig nodig.
